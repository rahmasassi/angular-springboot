pipeline {
    agent any

    stages {
        stage('Build Angular') {
            steps {
                echo "angular"
            }
        }

        stage('Build Spring Boot') {
            steps {
                echo "spring boot"
            }
        }

        stage('Deploy') {
            steps {
                echo "deploy"
            }
        }
    }

}